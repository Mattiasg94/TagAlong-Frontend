import React from "react";
import Styles from "../styles/css/LoadingSpinner.module.scss";

export default function LoadingSpinner() {
    return (
        <div>
            <div className={Styles.LoadingSpinner}>
            </div>
        </div>
    );
}